Wywołanie `make stat` kompiluje plik `stat.c` zawierający implementację przy pomocy funkcji `opendir()`, `readdir()`

Wywołanie `make nftw` kompiluje plik `nftw.c` zawierający implementację przy pomocy funkcji `nftw()`

Wywołanie `make clean` usuwa pliki wykonywalne