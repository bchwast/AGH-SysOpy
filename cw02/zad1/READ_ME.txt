Wywołanie `make lib` kompiluje plik `lib.c` zawierający implementację przy pomocy funkcji biblioteki C

Wywołanie `make sys` kompiluje plik `sys.c` zawierający implementację przy pomocy funkcji systemowych

Wywołanie `make clean` usuwa pliki wykonywalne